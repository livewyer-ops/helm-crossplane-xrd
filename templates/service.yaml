apiVersion: v1
kind: Service
metadata:
  name: {{ include "crossplane-xrd.fullname" . }}
  labels:
    {{- include "crossplane-xrd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crossplane-xrd.selectorLabels" . | nindent 4 }}
