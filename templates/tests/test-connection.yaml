apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crossplane-xrd.fullname" . }}-test-connection"
  labels:
    {{- include "crossplane-xrd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crossplane-xrd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
